<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Main Subdomains
    |--------------------------------------------------------------------------
    |
    | Поддомены, которые считаются главным сайтом и не привязаны к конкретному
    | региону. При заходе с этих поддоменов будет использован регион из
    | сессии/cookie или регион по умолчанию.
    |
    */

    'main_subdomains' => ['buldozer', 'www'],

    /*
    |--------------------------------------------------------------------------
    | Default Region ID
    |--------------------------------------------------------------------------
    |
    | ID региона по умолчанию, используется как fallback если в БД нет
    | региона с флагом default = true.
    |
    */

    'default_region_id' => 1,

    /*
    |--------------------------------------------------------------------------
    | Cookie Lifetime
    |--------------------------------------------------------------------------
    |
    | Время жизни cookie с регионом в минутах. По умолчанию 1 год.
    |
    */

    'cookie_lifetime' => 60 * 24 * 365,

];
