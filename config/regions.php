<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Main Subdomains (deprecated)
    |--------------------------------------------------------------------------
    |
    | Этот параметр больше не используется.
    | Основной домен определяется автоматически из APP_URL.
    | www обрабатывается как основной домен в middleware.
    |
    */

    'main_subdomains' => ['www'],

    /*
    |--------------------------------------------------------------------------
    | Default Region ID
    |--------------------------------------------------------------------------
    |
    | ID региона по умолчанию, используется как fallback если в БД нет
    | региона с флагом default = true.
    |
    */

    'default_region_id' => 1,

    /*
    |--------------------------------------------------------------------------
    | Cookie Lifetime
    |--------------------------------------------------------------------------
    |
    | Время жизни cookie с регионом в минутах. По умолчанию 1 год.
    |
    */

    'cookie_lifetime' => 60 * 24 * 365,

];
